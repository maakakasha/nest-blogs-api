services:
  postgres:
    image: postgres:15-alpine
    container_name: blog_api_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: blog
      POSTGRES_PASSWORD: blog_password
      POSTGRES_DB: blog_api
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U blog -d blog_api']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
